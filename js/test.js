function onActiveTabSize() {
	// 获取当前活动的tab页的body元素的宽度 (不含任何框架元素)
	//var w = $('#tt').tabs('getSelected').getInnerWidth();
	// 获取当前活动的tab页的body元素的高度 (不含任何框架元素)
	//var h = $('#tt').tabs('getSelected').getInnerHeight();
	// 获取当活动的tab页的id
	//var Atab = $('#tt').tabs('getSelected').id;

	// 获取组件
	//var submain = $(Atab);

	//if (submain) {
	//	$(Atab).setWidth(w);
	//	$(Atab).setHeight(h);
	//}
}